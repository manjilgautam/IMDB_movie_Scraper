const directors = [];

const tester = [];



function getItems(temp_array) {
    return tester.forEach(function(i, element) {
        const dirt = 'Hello'; // do some weird stuff here
        temp_array.push(dirt)
    });
}
